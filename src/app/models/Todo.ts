// This is a class with the attributes of ONE task or TODO item
// This contains the ID and Title as there can be multiple tasks with the same title so the ID will be unique
export class Todo
{
    id: number;
    title: string;
}